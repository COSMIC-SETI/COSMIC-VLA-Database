drivername: sqlite+pysqlite # database backend and driver name, such as postgresql+psycopg2
username: null # username string
password: null # password string
host: null # string hostname
port: null # integer port number
database: ":memory:" # string database name
query: null # an immutable mapping representing the query string. contains strings for keys and either strings or tuples of strings for values.